import CreateProgramLayout from '@/app/super-admin/programs/[folderID]/create/layout';

const Layout = ({ children }: React.PropsWithChildren) => <CreateProgramLayout>{children}</CreateProgramLayout>;

export default Layout;

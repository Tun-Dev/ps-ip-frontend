import EditProgramLayout from '@/app/super-admin/programs/[folderID]/edit/[programID]/layout';

const Layout = ({ children }: React.PropsWithChildren) => <EditProgramLayout>{children}</EditProgramLayout>;

export default Layout;
